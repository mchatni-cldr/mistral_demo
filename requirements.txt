# Versions verified against the live Cloudera AI deployment.
#
# fastmcp 4.x / mcp 2.x are REQUIRED, not cosmetic: Mistral's custom MCP
# connector requires the server to negotiate MCP protocol version 2025-06-18
# on `initialize`. fastmcp 2.9.2 (upstream's pin) only ever replies
# 2025-03-26, which Mistral rejects -- the Create button stays greyed out.
fastmcp==4.0.0
mcp==2.1.1
impyla==0.24.0
python-dotenv==1.2.3
starlette==1.6.0
uvicorn==0.52.4
